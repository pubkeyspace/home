/**
 * env.js is a local config file
 *
 * Create it from environmental variables with `make config`.
 */

module.exports = {
    // the not secret "secret" for the default FaunaDB website user
    FAUNADB_TOKEN: '%FAUNADB_TOKEN%'
}
